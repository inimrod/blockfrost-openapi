type: object
properties:
  image:
    type: string
    description: Can be either a URL to an image file or a base64 image data
    nullable: true
  ticker:
    type: string
    example: "RNR"
    nullable: true
  description:
    type: string
    nullable: true
  decimals:
    type: integer
    nullable: true
required:
  - image
  - ticker
  - description
  - decimals